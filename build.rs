fn main() {
    // let package = "plugin_test";
    // println!("cargo:rerun-if-changed={}", package);

    // let status = std::process::Command::new("cargo")
    //     .arg("build")
    //     .current_dir(package)
    //     .status()
    //     .expect("failed to execute cargo");

    // assert!(status.success(), "failed to build");
}